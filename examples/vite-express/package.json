{
  "name": "marko-vite-express-example",
  "description": "Sample app that demonstrates the power of building UI components using Marko, Vite, and Remix",
  "version": "1.0.0",
  "dependencies": {
    "marko": "^6.0.0"
  },
  "devDependencies": {
    "@marko/vite": "^5.4.4",
    "@types/express": "^5.0.6",
    "cross-env": "^10.1.0",
    "express": "^5.2.1",
    "rimraf": "^6.1.3",
    "vite": "^8.0.0"
  },
  "private": true,
  "scripts": {
    "build": "rimraf dist && cross-env NODE_ENV=production vite build --app",
    "dev": "vite",
    "preview": "npm run build && npm start",
    "start": "cross-env NODE_ENV=production node --enable-source-maps index.ts"
  },
  "type": "module"
}
